QUICK MOBILE FIX
================

1. SSH to server:
   ssh root@146.190.39.214
   cd /var/www/smart-doc-v2
   nano templates/voice-handsfree.html

2. Go to line 468 (audio onended):
   Ctrl+_
   468

3. After line 472 (isProcessing = false), add:
   micButton.classList.remove('processing');
   micIcon.textContent = '🎤';
   status.textContent = 'Tap to speak';

4. Also find where TTS might fail (around line 461):
   if (ttsResult.success) {
       // existing code...
   } else {
       // Add this else block:
       isProcessing = false;
       micButton.classList.remove('processing');
       micIcon.textContent = '🎤';
       status.textContent = 'Tap to speak';
   }

5. Save: Ctrl+X, Y, Enter

6. Restart:
   pm2 restart smart-doc-v2

This ensures the button resets even if TTS fails.